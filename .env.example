# HTTP 监听端口
PORT=8080

# Redis / Valkey URL（必填）
REDIS_URL=redis://busrzi-valkey:6379

# 客户端 JS 文件路径，默认 $WORKDIR/assets/client.js
# SCRIPT_PATH=assets/client.js

# TTL，默认 90 天，设置 0 禁用 TTL
#TTL_DAYS=90

# Debug 日志（仅 busrzi）
#RUST_LOG=busrzi=debug
